
// BEGIN (write your solution here)
export const addDigits = num => {
  let str = String(num);
  let result = 0;
  for (let i = 0; i < strings.length(str); i++) {
    result += Number(str[i]);
  if (strings.length(result) >1) {
    str = String(result);}
  else return result; 
  }
};
// END